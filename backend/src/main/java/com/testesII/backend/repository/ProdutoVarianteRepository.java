package com.testesII.backend.repository;

import com.testesII.backend.entity.ProdutoVariante;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ProdutoVarianteRepository extends JpaRepository<ProdutoVariante, Long> {
}
